{
  "name": "pr-review-dashboard",
  "version": "1.0.0",
  "description": "Dashboard to aggregate AI code review tool statistics",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@netlify/neon": "^0.1.0",
    "@octokit/rest": "^20.0.2",
    "string-similarity": "^4.0.4"
  }
}
